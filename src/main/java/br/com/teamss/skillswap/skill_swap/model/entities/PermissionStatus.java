package br.com.teamss.skillswap.skill_swap.model.entities;

public enum PermissionStatus {
    ALLOWED, // Permitido
    ALLOWED_DURING_USE, // Permitir somente durante o uso
    DENIED, // Negado
    NOT_SET // Não configurado
}